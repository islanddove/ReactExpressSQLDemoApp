{
  "name": "api",
  "version": "1.0.0",
  "engines": {
    "node": "13.10.x"
  },
  "description": "API. Used to serve the client code, as well as act as DB interface.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "heroku-postbuild": "cd Client/apple_picker_v2 && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "multer": "^1.4.2",
    "pg": "^7.18.2",
    "serve": "^11.3.0"
  }
}
