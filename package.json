{
  "name": "server",
  "version": "1.0.0",
  "engines": {
    "node": "13.10.x"
  },
  "description": "Server. Used to serve the client code, as well as act as DB interface.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.3",
    "multer": "^1.4.2",
    "pg": "^7.18.2",
    "serve": "^11.3.0"
  }
}
