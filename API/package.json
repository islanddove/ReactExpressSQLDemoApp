{
  "name": "api",
  "version": "1.0.0",
  "description": "API. Used to serve the client code, as well as act as DB interface.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "multer": "^1.4.2",
    "serve": "^11.3.0"
  }
}
